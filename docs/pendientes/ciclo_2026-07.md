# Pendientes — ciclo 2026-07

Tareas diferidas intencionalmente del ciclo 2026-06.
Revisar al inicio del ciclo julio antes de correr ningún módulo.

---

## 1. Renombrar archivos de retorno en todo el pipeline

**Por qué existe:** en ciclo 2026-06 se decidió que la columna informativa de devoluciones
se llama `RETORNO` (no `DEVOLUCION`, que ya estaba tomada por el crédito de exceso en Descuentos).
Por consistencia, los archivos deberían llamarse `pagos_yape_retorno.xlsx` y
`pagos_efectivo_retorno.xlsx`. El renombre se difirió para no bloquear la entrega del ciclo.

**Archivos a renombrar:**

| Archivo actual | Archivo nuevo | Módulo |
|---|---|---|
| `4_pagos/yape/motor_matching/outputs/pagos_yape_devolucion.xlsx` | `pagos_yape_retorno.xlsx` | motor_matching |
| `4_pagos/yape/motor_matching/docs/pagos_yape_devolucion_diseno.html` | `pagos_yape_retorno_diseno.html` | motor_matching docs |
| `5_cobranza/inputs/pagos_yape/pagos_yape_devolucion.xlsx` | `pagos_yape_retorno.xlsx` | 5_cobranza inputs |
| `5_cobranza/inputs/pagos_efectivo/pagos_efectivo_devolucion.xlsx` | `pagos_efectivo_retorno.xlsx` | 5_cobranza inputs |

**Código a cambiar:**

- `motor_matching/main.py` — constante `DEVOLUCION_FILE = "pagos_yape_devolucion.xlsx"` → `"pagos_yape_retorno.xlsx"`
- `motor_matching/exportar_motor.py` — función `exportar_pagos_devolucion` → `exportar_pagos_retorno` + referencias
- `5_cobranza/main.py` — constantes de lectura de archivos retorno
- `4_pagos/efectivo/crear_templates.py` — nombre del template si ya fue renombrado

**Riesgo:** bajo — es solo renombre de constantes y archivos. Sin cambios de lógica.

**Precaución:** hacer backup de `motor_matching/outputs/` antes de renombrar.
